%p#notice= notice
%h1 Orders
%table
  %thead
    %tr
      %th User
      %th Delivery
      %th Credit card
      %th Coupon
      %th Status
      %th{:colspan => "3"}
  %tbody
    - @orders.each do |order|
      %tr
        %td= order.user
        %td= order.delivery
        %td= order.credit_card
        %td= order.coupon
        %td= order.status
        %td= link_to 'Show', order
        %td= link_to 'Edit', edit_order_path(order)
        %td= link_to 'Destroy', order, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Order', new_order_path
