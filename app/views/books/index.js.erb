$('#books-container').append("<%= j(render 'books', books: @books) %>");

<% if @books.last_page? %>
  $('#load-more-posts').hide();
<% else %>
  $("#load-more-posts").attr("href", "<%= books_path(page: (@books.current_page + 1)) %>");
<% end %>